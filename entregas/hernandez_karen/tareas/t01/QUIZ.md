# QUIZ T01 · Karen Hernández
## 1. Atributo contra variable
Un atributo son las caracteristicas de los objetos que creamos,es el estado del objeto y son caracteristicas que todos los objetos de esa clase que creamos tendran,por ejemplo en mi tarea,un estuche tendra un pila diferente, una vaso de agua una cantidad de agua duferente y en la tarjeta un saldo diferente.
## 2. Constructor: que es, cuando corre, cuantas veces
// firma: TarjetaMetrobus(int) · mismo nombre de la clase · SIN tipo de retorno
TarjetaMetrobus(String numeroSerie){
this.numeroSerie = numeroSerie; 
}
El constructor se ejecuta cada vez que creamos un objeto nuevo con new, entonces si creo tres tarjetas nuevas, se ejecuta 3 veces ese constructor, en mi caso lo que hace es que cada tarjeta tenga un numero diferente de serie para poder diferenciarse.

## 3. Para que sirve this
Dentro del constructor nos ayuda a que java pueda diferecniar a los parametros que tiene el constructor y a los atributos en general de los objetos, el que sigue despues del punto es el atributo y el que va despues del = es el parametro del constructor
## 4. La firma de un metodo
//firma: guardar()
//firma: pagarTarifa()
La firma de los  metodos nos indica el nombre del metodo y el tipo de dato de los parametros que recibe o si no recibe parametros
## 5. Parametro y argumento
El parametro es
## 6. Que significa void
(tu respuesta)
## 7. Primitivo contra referencia, y el null
(tu respuesta)
## 8. Por que 7 / 2 da 3
(tu respuesta)
## 9. El casting y lo que se pierde
(tu respuesta, con tu linea)
## 10. new, clase e instancia
(tu respuesta)